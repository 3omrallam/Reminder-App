import { TaskControllersDirective } from './task-controllers.directive';

describe('TaskControllersDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskControllersDirective();
    expect(directive).toBeTruthy();
  });
});
